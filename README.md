# THIS PROJECTO IS UNDER CONSTRUCTION.

# k8s platform Gitops.

Nexa Platform is a `cloud-native` platform for the deployment and operation of microservices based on a `GitOps architecture`. It provides a standardized foundation for the development, containerization, orchestration, and observability of services running on `Kubernetes`, with automated deployments managed via `ArgoCD`.
